<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title></title>
</head>

<body>
  <h1>Weeks index page</h1>
  <ul>
    <% weeks.forEach(function(week) { %>
    <li>
      The <a href="/weeks/<%= week._id; %>">week</a> starts on <%= week.startDay %> and ends on <%= week.endDay %>.
      The week is in the month of <%= week.startDay.getMonth() %> and the year of <%= week.endDay.getFullYear() %>.

      <!-- ADD DELETE FORM HERE -->
      <form action="/weeks/<%= week._id%>?_method=DELETE" method="POST">
        <input type="submit" value="DELETE" />
      </form>
      <!-- weeks/5e5a93cd12675b4c0efcb17e/edit -->
      <a href="/weeks/<%= week._id%>/edit">Edit</a>
    </li>
    <% }); %>
  </ul>

  <br><br>

  <a href="/weeks/new">Add New Week</a>

</body>

</html>